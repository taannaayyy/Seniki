import PagePlaceholder from '../components/PagePlaceholder'

function NotFound() {
  return (
    <PagePlaceholder
      title="Not found"
      note="That page doesn't exist. Pick a section from the sidebar."
    />
  )
}

export default NotFound
