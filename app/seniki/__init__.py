"""Seniki backend: the user's own data, exposed to the frontend over HTTP.

Today that is one source — the iCloud calendar in `icloud` — surfaced by the
FastAPI app in `api`. Run it with `python -m seniki` from the `app/` directory.
"""
